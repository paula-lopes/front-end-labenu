import Router from "routes/Router";
import { primaryColor } from "constants/colors";

export const App = () => {
   console.log(primaryColor)
  return (
    <>
      <Router />
    </>
  );
};
export default App;
